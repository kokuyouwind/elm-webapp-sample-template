{
  "name": "elm-webapp-sample-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "elm": "^0.19.1-3"
  },
  "devDependencies": {
    "elm-live": "^4.0.1",
    "elm-test": "^0.19.1-revision2",
    "http-server": "^0.12.0"
  },
  "scripts": {
    "start": "elm-live src/Main.elm --open -u",
    "test": "elm-test",
    "watch": "elm-test --watch",
    "make": "cp -r static dists && elm make src/Main.elm --output dists/elm.js --optimize",
    "serve": "http-server dists -o",
    "clean": "rm -rf dists/*"
  },
  "author": "",
  "license": "ISC"
}
